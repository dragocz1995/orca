import { ShieldAlert } from 'lucide-react';
import type { ModuleMeta } from '../types';

export const meta: ModuleMeta = {
  id: 'escalations',
  label: 'Escalations',
  route: '/escalations',
  icon: ShieldAlert,
  group: 'Operate',
};
